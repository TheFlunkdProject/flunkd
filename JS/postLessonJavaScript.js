document.getElementById('currentTopic2').innerHTML = title;
document.getElementById('currentTopic1').innerHTML = title;

MathJax.Hub.Queue(["Typeset",MathJax.Hub]);//because now the practice problem is loaded in